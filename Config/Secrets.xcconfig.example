// Copy to Secrets.xcconfig and replace with your Amplitude API key.
// Secrets.xcconfig is gitignored — never commit real keys.
//
// Get your key: Amplitude dashboard → Settings → Projects → [your project] → API Key

AMPLITUDE_API_KEY = your_amplitude_api_key_here
